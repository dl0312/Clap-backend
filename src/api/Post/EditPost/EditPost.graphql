type EditPostResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  EditPost(
    postId: Int!
    title: String
    titleImg: String
    titleImgPos: Float
    categoryId: Int
    body: String
  ): EditPostResponse!
}
