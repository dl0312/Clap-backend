type AddPostResponse {
  ok: Boolean!
  error: String
  postId: Int
}

type Mutation {
  AddPost(
    title: String!
    titleImg: String
    titleImgPos: Float
    tags: [String]
    body: String!
    gameId: Int!
  ): AddPostResponse!
}
