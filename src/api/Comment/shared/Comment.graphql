type Comment {
  id: Int!
  body: String!
  userId: Int
  user: User!
  postId: Int
  post: Post!
  parentCommentId: Int
  parentComment: Comment
  createdAt: String!
  updatedAt: String
}
