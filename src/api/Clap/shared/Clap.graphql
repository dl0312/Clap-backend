type Clap {
  id: Int!
  senderId: Int
  sender: User!
  receiverId: Int
  receiver: User!
  postId: Int
  post: Post
  wikiImageId: Int
  wikiImage: WikiImage
  createdAt: String!
  updatedAt: String
}
