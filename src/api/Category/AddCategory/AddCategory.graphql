type AddCategoryResponse {
  ok: Boolean!
  error: String
  categoryId: Int
}

type Mutation {
  AddCategory(
    name: String!
    parentIds: [Int]
    childrenIds: [Int]
  ): AddCategoryResponse!
}
