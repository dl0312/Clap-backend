type WikiImage {
  id: Int!
  name: String
  userId: Int!
  user: User!
  category: Category!
  categoryId: Int!
  shownImageId: Int
  shownImage: ShownImage
  hoverImage: String!
  posts: [Post]
  postsCount: Int!
  claps: [Clap]
  clapsCount: Int!
  createdAt: String!
  updatedAt: String
}
