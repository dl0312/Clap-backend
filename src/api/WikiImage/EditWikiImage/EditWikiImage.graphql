type EditWikiImageResponse {
  ok: Boolean
  error: String
}

type Mutation {
  EditWikiImage(
    wikiImageId: Int!
    categoryId: Int
    name: String
    shownImageId: Int
    hoverImage: String
  ): EditWikiImageResponse!
}
