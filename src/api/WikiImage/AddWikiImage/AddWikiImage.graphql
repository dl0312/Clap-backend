type AddWikiImageResponse {
  ok: Boolean
  error: String
}

type Mutation {
  AddWikiImage(
    categoryId: Int!
    name: String
    shownImageId: Int!
    hoverImage: String!
  ): AddWikiImageResponse!
}
