type AddWikiImageResponse {
  ok: Boolean
  error: String
}

type Mutation {
  AddWikiImage(
    categoryId: Int!
    name: String
    shownImage: String!
    hoverImage: String!
  ): AddWikiImageResponse!
}
