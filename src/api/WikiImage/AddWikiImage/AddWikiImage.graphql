type AddWikiImageResponse {
  ok: Boolean
  error: String
  wikiImageId: Int
}

type Mutation {
  AddWikiImage(
    categoryId: Int!
    name: String
    shownImageId: Int!
    hoverImage: String!
  ): AddWikiImageResponse!
}
