type GetWikiImageByIdResponse {
  ok: Boolean!
  error: String
  wikiImage: WikiImage
  isClapped: Boolean
  isMine: Boolean
}

type Query {
  GetWikiImageById(wikiImageId: Int!): GetWikiImageByIdResponse!
}
